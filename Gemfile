source 'https://rubygems.org'

group :jekyll_plugins do
  gem 'jekyll-feed'
  gem 'jekyll-sitemap'
  gem 'jekyll-redirect-from'
  gem 'jemoji'
  gem 'webrick', '~> 1.8'
end

gem 'github-pages'
gem 'connection_pool', '2.5.0'



#source 'https://rubygems.org'

# GitHub Pages manages all Jekyll + plugin versions.
#gem "github-pages", group: :jekyll_plugins

# Needed for local `jekyll serve` only
#gem "webrick"

# Remove these because github-pages already controls them:
# gem 'jekyll-feed'
# gem 'jekyll-sitemap'
# gem 'jekyll-redirect-from'
# gem 'jemoji'
# gem 'connection_pool'